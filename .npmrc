# angular-tabler-icons@3.26 declares a stale peer range (@angular <=19) but its
# runtime APIs (input signals, linkedSignal) are forward-compatible with Angular 20.
# Allow the install to proceed despite that advisory peer mismatch.
legacy-peer-deps=true
